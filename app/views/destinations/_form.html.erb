<%= form_for @destination, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :aiport_code, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :aiport_code, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :airport_name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :airport_name, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                destinations_path, :class => 'btn' %>
  </div>
<% end %>
